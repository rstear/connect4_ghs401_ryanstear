import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class BigRing here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class BigRing extends Actor
{
    /**
     * Act - do whatever the BigRing wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
    
    public BigRing(){
        
        getImage().scale(130,130);
        //image.scale((int)(image.getWidth()*0.2), (int)(image.getHeight()*0.2));
        //setImage(image);
        
        
    }
}
